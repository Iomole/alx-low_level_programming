#include <stdio.h>
/**
 * main - main function to print 0 to 9
 * Return: return zero for success
 */
int main(void)
{
	int n;

	for (n = 0; n < 10; n++)
		printf("%d", n);
	printf("\n");
	return (0);
}
