#include "main.h"

/**
 * _abs - A function that computes the absolute value of an integer
 * @n: parameter (the value to compute)
 * Return: the compute value
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	return (-n);
}

