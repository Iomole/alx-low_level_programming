#include "main.h"

/**
 * _isdigit - A program to checks for a digit (0 to 9)
 * @c: int parameter
 * Return: To 1 if c is a digit, 0 otherwise
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');
}

